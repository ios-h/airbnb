spring:

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
    defer-datasource-initialization: true

  #  추가해야 data.sql실행
  sql:
    init:
      mode: always

  config:
    import: application-testdb.yml

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace
